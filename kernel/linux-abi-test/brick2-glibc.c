#include <stdio.h>
int main(void) {
    printf("Hello from a static GLIBC printf(), on the pure-Rust kernel!\n");
    printf("Linux ABI brick 2: a real libc program runs natively.\n");
    return 0;
}
